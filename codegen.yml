watch:  true
overwrite: true
schema: "schema/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
      contextType: src/api/context#IContext
      enumValues:
        Gender: '@db/cat#Gender'
      mappers:
        User: '@db/user#IUser'
        Cat: '@db/cat#ICat'